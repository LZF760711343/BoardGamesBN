declare namespace qqgame {
    function login(data:string);
    function pay(data: string);
    function share(data: string);
    function getFriends(data: string);
    function refreshToken2(data: string);
}
